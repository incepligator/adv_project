package com.guffy.rest;

import com.guffy.vo.AmazonVO;

public class ProductValidation {

	/*
	 * boolean validationCategory(AmazonVO vo) {
	 * 
	 * boolean validate = false;
	 * 
	 * ProductCategory[] list = ProductCategory.values();
	 * 
	 * int i = 0;
	 * 
	 * while (list.length > i) {
	 * 
	 * if ((list[i]).toString().equalsIgnoreCase(vo.getCategory())) {
	 * 
	 * validate = true; break;
	 * 
	 * } i++;
	 * 
	 * } return validate; }
	 */
}
